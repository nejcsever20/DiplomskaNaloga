@page
@using diplomska.Properties
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">@Resources.error</h1>
<h2 class="text-danger">@Resources.erorr_processing</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>@Resources.development_mode</h3>
<p>
    Swapping to <strong>@Resources.development</strong> environment will display more detailed information about the error that occurred.
</p>
<p>
    <strong>Development environment should not be enabled in deployed applications</strong>, as it can result in sensitive information from exceptions being displayed to end users. For local debugging, development environment can be enabled by setting the <strong>@Resources.apsnetcore_environment</strong> environment variable to <strong>@Resources.development</strong>, and restarting the application.
</p>
